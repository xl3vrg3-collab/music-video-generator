# LUMN 10-Agent Full-Site Audit — 2026-04-16

Audit scope: entire LUMN web app, all endpoints, all stages, security, a11y, persistence.
Method: 10 parallel code-review agents (no UI interaction) + Playwright MCP expansion test.

## VOTE TALLY — top items (sorted by convergence + priority)

### CRITICAL (must fix before shipping externally)

| # | Finding | Agent | File | Impact |
|---|---------|-------|------|--------|
| 1 | **Prop `/photo` endpoint missing** — UI calls it (index.html:15534), server.py has no handler | 3 | server.py | Prop photo upload 404s today |
| 2 | **Prop `/describe` endpoint missing** — UI calls it (index.html:15558), server.py has no handler | 3 | server.py | Prop AI Fill 404s today |
| 3 | **Async generation rate limit check is post-enqueue** — user can enqueue 100 jobs before limit hits | 8 | server.py:13849, 13703 | $$$ attack vector |
| 4 | **4-layer prompting silent drop** — `compile_shot_prompt` only uses `surface`; symbolic/emotional/hidden stored but never compiled | 5 | lib/prompt_assembler.py:664-667 | BUG 9 confirmed, UI theatre |
| 5 | **Gemini edit photorealistic override** — ref photo locks output style, ignores "anime" / "noir" keyword | 2 | server.py:8954, 9059 | BUG 3, breaks anime projects |

### HIGH

| # | Finding | Agent | Fix |
|---|---------|-------|-----|
| 6 | Brief form fields not autosaved — data loss on refresh | 1 | Add adStoryline/adStyle/globalWorld to `_getFullProjectState` |
| 7 | Brief data never written to project_style.json (BUG 4) | 1 | Persist `{storyline, world, style, lyrics, film_*}` on pipeline start |
| 8 | No retry on fal.ai transient failures (today's 2.2 DNS kill) | 6 | Exponential backoff in `_fal_submit` + `_download_file` |
| 9 | `try/except` around `resolve_presets` swallows errors silently | 5 | Remove blanket except, log to stderr |
| 10 | Cascade delete missing — scenes keep orphaned char/env/costume IDs | 4 | Clear refs in `delete_*` methods |
| 11 | Logout doesn't clear route persistence — User B sees User A state | 9 | Clear all 5 LS_* keys in `_onAuthLogout` |
| 12 | DELETE project blocked for non-active projects (contradicts comment) | 7 | Remove X-Lumn-Project enforcement on delete |
| 13 | localStorage globals (`lumn_welcomed`, `lumn_mode`) not slug-scoped | 7 | Wrap with `_sk()` |
| 14 | Concurrency unbounded — 26 Kling jobs can fire simultaneously | 6 | ThreadPoolExecutor(max_workers=2) |
| 15 | Symlink gap in `_safe_user_path` — validated path passed to fal.ai without lstat | 8 | Add `os.path.islink()` check |
| 16 | Character lock doesn't disable all fields (missing `<details>` collapsibles) | 2 | Systematize: disable every input inside form container |
| 17 | Server-side lock NOT enforced on `/describe` — UI disables button only | 2 | Add 423 Locked check |
| 18 | Async jobs don't inherit rate-limit status from sync jobs | 8 | Unified limiter |
| 19 | Toast container has no `aria-live` — screen readers miss errors | 10 | Add `aria-live="assertive"` |
| 20 | No slug validation — user can create "vault", "default", "output" | 7 | Reserved-name deny list |

### MEDIUM

- Duration dropdown capped 2min, no song auto-detect (BUG 5 confirmed, Agent 1)
- No per-shot Kling tier/negative prompt UI (Agent 5)
- Text-dim CSS contrast 4.24:1 (fails WCAG AA), fonts 9-10px everywhere (Agent 10)
- 50+ icon-only buttons without aria-label (Agent 10)
- Browser `confirm()` dialogs for destructive actions (Agent 10)
- Scene form silent-drops unknown fields (same pattern as the bug we just fixed; Agent 4)
- No POST `/api/pos/scenes/reorder` endpoint (Agent 4)
- No v6 DELETE endpoints (anchors/clips grow unbounded) (Agent 6)
- Prop form field array `propFormIds` likely incomplete vs costume's 15 fields (Agent 3)
- Rename project doesn't migrate project_style.json to new slug path (Agent 1)
- Cross-tab entity delete race — no `storage` event listener (Agent 9)
- Framing / movement / lens / subject_position UI fields written but `compile_shot_prompt` never reads them (Agent 5)
- Login rate limit uses CRC32(IP) — /24 collisions allow credential stuffing (Agent 8)
- `_projectsReady` resolves on fetch failure, leaving `_projects=[]` (Agent 7)

### LOW
See individual agent reports (archived in this file's history).

## FULL AGENT REPORTS

(Raw findings preserved for reference. Each agent capped at 10 findings.)

### Agent 1 — Brief stage
10 findings. Top 3: autosave brief fields, persist to project_style.json, extend duration 600s + auto-detect.

### Agent 2 — Characters + Voices
10 findings. Top 3: photorealistic ref-photo override (BUG 3), server-side lock on `/describe`, systematize form disabling.

### Agent 3 — Env/Costume/Prop
10 findings. Top 3: **Prop /photo endpoint missing (CRITICAL), Prop /describe endpoint missing (CRITICAL),** propFormIds parity.

### Agent 4 — Script (scenes)
10 findings. Top 3: cascade delete scenes on asset delete, validate energy/duration/name, reorder endpoint.

### Agent 5 — Shots compile_shot_prompt
10 findings. Top 3: wire 4 layers, remove blanket try/except, add per-shot Kling tier + negative prompt.

### Agent 6 — Render + v6 endpoints
10 findings. Top 3: retry logic on fal calls, concurrency limit, v6 DELETE routes + cleanup job.

### Agent 7 — Project switcher + vault
10 findings. Top 3: DELETE non-active project, slug validation, scope all localStorage to slug.

### Agent 8 — Auth / CSRF / security
10 findings. Top 3: **async rate limit bypass (CRITICAL),** symlink gap in `_safe_user_path`, rate limit by user_id not CRC32(IP).

### Agent 9 — Route persistence
10 findings. Top 3: clear LS keys on logout + project delete, form draft autosave, storage event listener.

### Agent 10 — Accessibility
10 findings. Top 3: contrast + font sizes, aria-label all icon buttons, replace `confirm()` with custom modal.

## Total: ~100 findings across 10 angles.

## RECOMMENDATION

Post-MV, address in this order (based on convergence of high-priority votes + blast radius):

**Tier 1 — blocking functionality (1 day):**
1. Add Prop `/photo` + `/describe` endpoints (Agent 3 CRITICAL)
2. Fix `compile_shot_prompt` silent drop — same 20-min pattern as scenes fix (Agent 5)
3. Fix Gemini photorealistic override (Agent 2)

**Tier 2 — security + money (half day):**
4. Fix async rate limit bypass (Agent 8)
5. Add retry on fal.ai transient failures (Agent 6)
6. Add symlink rejection (Agent 8)

**Tier 3 — data integrity (half day):**
7. Cascade delete scenes on asset delete (Agent 4)
8. Clear route keys on logout + project delete (Agent 9)
9. Scope all localStorage to slug (Agent 7)

**Tier 4 — UX cleanup (1 day):**
10. Brief autosave + persist to project_style.json (Agent 1)
11. A11y contrast + fonts + aria-labels (Agent 10)
12. Replace `confirm()` with custom modal (Agent 10)

Everything else (Tier 5 — nice-to-have): see MEDIUM list above.

---

## PLAYWRIGHT MCP EXPANSION TEST — new findings (not caught by code review)

Method: logged in as `mctest@local`, clicked each of the 5 stage pills, ran JS to open every `<details>` and click every `aria-expanded="false"` element, then scanned console errors + DOM.

### CRITICAL (UI-server contract breaks)

| # | Finding | Evidence | Impact |
|---|---------|----------|--------|
| E1 | **`GET /api/scenes` returns 404** — UI calls it, no server handler | console 404, called on initial project load | Some scene data path broken; unclear what calls it |
| E2 | **`GET /api/pos/shots` returns 404** — UI calls it, no server handler | console 404 | Shot listing silently fails |
| E3 | **`/api/cost` poll has no backoff** — 120+ ERR_CONNECTION_REFUSED firing when server restarts | console flood | CPU burn, noisy logs, bad UX on server restart |

### HIGH (UX/architecture)

| # | Finding | Evidence | Impact |
|---|---------|----------|--------|
| E4 | **All 5 stages render into DOM simultaneously** — 644–653 buttons, 15 `<video>`, 46 `<img>` on any stage | `document.querySelectorAll('button').length` at stages 4/5 | Memory bloat, slow layout, event handler collisions, test fragility |
| E5 | **Project title / data mismatch** — title textbox "TB - Lifestream Static" but stats show `Scenes: 3` (real TB has 26) | stats bar | Users can't trust which project is active |
| E6 | **Stage-1 stats change after first stage-click** (`Characters 1 → 0`, `Scenes 26 → 3`) | before/after stage-2 click | Initial load reads wrong project; state hydration race |
| E7 | **Deleted character still referenced in UI state** — 404 on `api/pos/characters/6d31f281-4cc/preview` + `/photo` | console 404 | Stale DOM refs after entity delete (matches Agent 4 "cascade delete" finding, but also leaks client-side) |

### MEDIUM (stale data, over-build)

| # | Finding | Evidence | Impact |
|---|---------|----------|--------|
| E8 | **Stale Runway refs persist in project data** — `/AppData/Local/Temp/runway_scene_*.png` 404s (x3) | console | Legacy from Runway→fal migration (we're fal-only per memory rule); data never cleaned |
| E9 | **20 dead `/tmp/mock_anchor_b*_s*.png` refs** | console x20 | Mock data leaked into real project state |
| E10 | **Two parallel character UIs with conflicting counts** — "Preproduction Packages" shows 6 (Buddy/Owen/Maya/Yara Osel/PIP/MOTE), character list below shows "0 characters" | stage-2 screenshot | Over-build — either consolidate or sync state |
| E11 | **"Auto Director" banner persists across all 5 stages** — Brief-only UI showing on Render / Export | screenshots | Visual clutter, off-topic CTA |

### Raw stats per stage (DOM size — stacked content)

| Stage | Step | `<details>` | Buttons | Videos | Imgs |
|-------|------|-------------|---------|--------|------|
| 1 Brief | 1/5 | 54 expanded | — | — | — |
| 2 Cast | 2/5 | +7 | — | — | 41 cards |
| 3 Shots | 3/5 | +47 | — | — | 148 cards |
| 4 Render | 4/5 | — | **644** | 15 | — |
| 5 Export | 5/5 | — | **653** | 15 | 46 |

**Key insight:** Button count grows across stages because old stage DOM is kept mounted. This explains several memory/perf complaints and is consistent with the Agent 5 vote to "simplify Shots UI" — the over-build is global, not local.

---

## COMBINED PRIORITY — code audit + expansion test

1. Fix Prop `/photo` + `/describe` (Agent 3) + `/api/scenes` + `/api/pos/shots` (E1/E2) — all "UI calls endpoint that doesn't exist" pattern; grep `fetch\(` for full map.
2. Unmount inactive stages (E4) — one-line React fix, kills memory bloat and many UI flakiness bugs downstream.
3. Project title↔data sync (E5/E6) — either redirect on project-switch or block UI until `_projectsReady` resolves.
4. Client-side cache invalidation on entity delete (E7) — covers cascade delete from server too.
5. Cost-poll backoff (E3) — 30s → 60s → 120s on 5xx/net errors.
6. One-time cleaner for stale Runway + mock refs (E8/E9).

